<Page
    x:Class="EgyenlitoWin8.FacebookView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EgyenlitoWin8"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModels="using:EgyenlitoPortableLIB.ViewModels"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d">

    <Page.DataContext>
        <ViewModels:PDFReaderViewModel />
    </Page.DataContext>
    
    <Grid Background="White"
          Loaded="Grid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="3*" />
        </Grid.RowDefinitions>

        <Rectangle Grid.Row="0" 
                   Fill="#00a8c9" />
        <Image Grid.Row="0"
               Source="/Images/logo.png"
               MaxHeight="200" />
        
        <WebView Grid.Row="1"
                 x:Name="Webview"
                 LoadCompleted="Webview_LoadCompleted">
            <Interactivity:Interaction.Behaviors>
                <Core:EventTriggerBehavior EventName="LoadCompleted">
                    <Core:InvokeCommandAction Command="{Binding Post}" />
                </Core:EventTriggerBehavior>
            </Interactivity:Interaction.Behaviors>
        </WebView>
    </Grid>
</Page>
